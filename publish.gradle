apply plugin: 'maven'

String getArtifactId() {
    return project.getName()
}

uploadArchives {
    repositories.mavenDeployer {
        repository(url: uri("D:\\workspace\\repo"))
        pom.groupId = 'com.twlk.http'
        pom.artifactId = getArtifactId()
        pom.version = "1.0.0"
    }
}